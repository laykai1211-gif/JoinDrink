server:
  port: 8081

spring:
  security:
    user:
      name: none
  servlet:
    multipart:
            # 單一檔案的大小限制 (例如：一張圖片最大 10MB)
      max-file-size: 10MB
            # 單次請求的總大小限制 (例如：一次上傳 5 張，總計不能超過 50MB)
      max-request-size: 50MB

  datasource:
    # ... 保持原樣 (MySQL 設定) ...
    url: jdbc:mysql://mysql-3c893072-a901211-ba4b.a.aivencloud.com:10093/defaultdb?ssl-mode=REQUIRED&characterEncoding=utf8&serverTimezone=UTC
    username: avnadmin
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


# 💡 1. 新增 Firebase 設定 (確保後端能找到你的金鑰 JSON)
firebase:
  config-path: "classpath:serviceAccountKey.json"

app:
  reset_password_url: "http://localhost:5173/Resetpassword"
  sms:
    mode: "mock" # ✅ 設定為 real/mock 是正確的

jwt:
  secret: ThisIsASuperLongSecretKeyForDrinkingApp2026ThatMustBeAtLeast64CharactersLongToSatisfyHS512Requirements!
  expiration: 86400000
