server:
  port: 8081

spring:
  security:
    user:
      name: none

  datasource:
    # ... 保持原樣 (MySQL 設定) ...
    url: jdbc:mysql://mysql-3c893072-a901211-ba4b.a.aivencloud.com:10093/defaultdb?ssl-mode=REQUIRED&characterEncoding=utf8&serverTimezone=UTC
    username: avnadmin
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

# 💡 1. 徹底刪除或註解掉 Twilio (你已經不需要它了，留著反而容易報錯)
# twilio: ... (已移除)

# 💡 2. 新增 Firebase 設定 (確保後端能找到你的金鑰 JSON)
firebase:
  config-path: "classpath:serviceAccountKey.json"

app:
  reset_password_url: "http://localhost:5173/Resetpassword"
  sms:
    mode: "real" # ✅ 設定為 real/mock 是正確的